**实验室工作目的：** 根据最小二乘法，找到作为给定表格函数最佳逼近的函数。

实验室工作由两部分组成：计算部分和程序部分。

实验室作业的变体编号根据ISU中的小组名单序号确定。

## 工作执行顺序

## 任务的计算机实现

实验室工作的计算部分只需包含在报告中。

**任务：**

1. 在指定区间内（见表1）形成给定函数的制表表格。
2. 根据指定区间的11个点构建线性和二次逼近；
3. 求出每个逼近函数的均方根偏差。答案保留三位小数；
4. 选择最佳逼近；
5. 构建给定函数以及获得的线性和二次逼近的图形；
6. 在报告中提供详细计算过程。

## 任务的程序实现

研究中使用：
•    线性函数，
•    2次多项式函数，
•    3次多项式函数，
•    指数函数，
•    对数函数，
•    幂函数。

1. 计算所有研究函数的偏差度量：S = ∑_{i=1}^{n} [φ(x_i) − y_i]^2；
2. 通过最小化函数S来细化经验函数的系数值；
3. 形成假设经验依赖关系的数组（φ(x_i), ε_i）；
4. 确定每个逼近函数的均方根偏差。选择最小值，从而选择最佳逼近；
5. 构建所得经验函数的图形。

## 研究方法：

1. 计算所有研究函数的偏差度量：S = ∑_{i=1}^{n} [φ(x_i) − y_i]^2；
2. 通过最小化函数S来细化经验函数的系数值；
3. 形成假设经验依赖关系的数组（φ(x_i), ε_i）；
4. 确定每个逼近函数的均方根偏差。选择最小值，从而选择最佳逼近；
5. 构建所得经验函数的图形。

## 任务：

1. 提供从文件/控制台输入初始数据的功能（y = f (x) 表应包含8到12个点）；
2. 实现最小二乘法，研究所有指定的函数；
3. 提供将结果输出到文件/控制台的功能：逼近函数的系数，均方根偏差，x_i, y_i, φ(x_i), ε_i 的值数组；
4. 对于线性依赖关系，计算皮尔逊相关系数；
5. 计算决定系数，程序根据得到的 R^2 值输出相应的消息；
6. 程序应显示最佳逼近函数；
7. 组织函数图形的输出，图形应完整显示整个研究区间（留有余量）；
8. 程序应在各种数据集（包括不正确的数据）上进行测试。

## 报告要求和内容

报告应包含以下部分：

• 工作目的，
• 方法的计算公式，
• 实验室工作的计算部分，
• 程序清单（至少包括所用方法的代码），
• 逼近函数的图形，
• 不同初始数据下程序执行的结果（不少于三个），
• 结论。

## 任务选项

**表1. 计算实现任务的任务选项**

| 变体编号 |            函数            |         研究区间         | 变体编号 |            函数            |         研究区间         |
| :------: | :------------------------: | :-----------------------: | :------: | :------------------------: | :-----------------------: |
|    1    | \(y=\frac{12x}{x^{4}+1}\) | \(x\in [0,2]\) \(h=0,2\) |    15    | \(y=\frac{4x}{x^{4}+15}\) | \(x\in [-2,0]\) \(h=0,2\) |
|    2    | \(y=\frac{15x}{x^{4}+2}\) | \(x\in [0,4]\) \(h=0,4\) |    16    | \(y=\frac{17x}{x^{4}+16}\) | \(x\in [-4,0]\) \(h=0,4\) |
|    3    |  \(y=\frac{4x}{x^{4}+3}\)  | \(x\in [-2,0]\) \(h=0,2\) |    17    | \(y=\frac{2x}{x^{4}+17}\) | \(x\in [0,2]\) \(h=0,2\) |
|    4    | \(y=\frac{15x}{x^{4}+4}\) | \(x\in [-4,0]\) \(h=0,4\) |    18    | \(y=\frac{30x}{x^{4}+18}\) | \(x\in [0,4]\) \(h=0,4\) |
|    5    |  \(y=\frac{6x}{x^{4}+5}\)  | \(x\in [0,2]\) \(h=0,2\) |    19    | \(y=\frac{5x}{x^{4}+19}\) | \(x\in [0,2]\) \(h=0,2\) |
|    6    | \(y=\frac{12x}{x^{4}+6}\) | \(x\in [0,2]\) \(h=0,2\) |    20    | \(y=\frac{11x}{x^{4}+20}\) | \(x\in [0,4]\) \(h=0,4\) |
|    7    | \(y=\frac{23x}{x^{4}+7}\) | \(x\in [-2,0]\) \(h=0,2\) |    21    | \(y=\frac{14x}{x^{4}+21}\) | \(x\in [-4,0]\) \(h=0,4\) |
|    8    |  \(y=\frac{3x}{x^{4}+8}\)  | \(x\in [-2,0]\) \(h=0,2\) |    22    | \(y=\frac{5x}{x^{4}+22}\) | \(x\in [-2,0]\) \(h=0,2\) |
|    9    |  \(y=\frac{4x}{x^{4}+9}\)  | \(x\in [0,2]\) \(h=0,2\) |    23    | \(y=\frac{16x}{x^{4}+23}\) | \(x\in [0,4]\) \(h=0,4\) |
|    10    | \(y=\frac{18x}{x^{4}+10}\) | \(x\in [0,4]\) \(h=0,4\) |    24    | \(y=\frac{7x}{x^{4}+24}\) | \(x\in [-4,0]\) \(h=0,4\) |
|    11    | \(y=\frac{5x}{x^{4}+11}\) | \(x\in [-2,0]\) \(h=0,2\) |    25    | \(y=\frac{28x}{x^{4}+25}\) | \(x\in [0,4]\) \(h=0,4\) |
|    12    | \(y=\frac{4x}{x^{4}+12}\) | \(x\in [-2,0]\) \(h=0,2\) |    26    | \(y=\frac{7x}{x^{4}+26}\) | \(x\in [0,4]\) \(h=0,4\) |
|    13    | \(y=\frac{31x}{x^{4}+13}\) | \(x\in [0,4]\) \(h=0,4\) |    27    | \(y=\frac{18x}{x^{4}+27}\) | \(x\in [0,2]\) \(h=0,2\) |
|    14    | \(y=\frac{25x}{x^{4}+14}\) | \(x\in [0,4]\) \(h=0,4\) |    28    | \(y=\frac{21x}{x^{4}+28}\) | \(x\in [-4,0]\) \(h=0,4\) |

1. 逼近表格函数的必要性是什么？
2. 逼近与插值有何不同？
3. 阐述逼近问题。
4. 如何选择逼近方程的类型？
5. 解释最小二乘法（LSM）的本质。
6. 什么是偏差度量以及如何计算它？
7. 最小二乘法归结为解决什么问题？
8. 阐述使用最小二乘法进行多项式逼近的问题。
9. 什么是线性和二次逼近？
10. 给出线性和二次逼近的图形解释？
11. 什么是均方根偏差？
12. 如何使用非多项式函数进行数据逼近？
13. 如何评估所得逼近的质量？
14. 如何选择最佳逼近函数？
15. 在研究范围之外使用逼近方程是否合适？


