# ОПИ 2025/2026

## Билет 1 · 变体 1

### 第1题（原题 1）：ISO/IEC 12207:2010: Жизненный цикл ПО. Группы процессов ЖЦ.

**俄语答题 / Русский ответ**

Жизненный цикл ПО — период от возникновения потребности в системе до её вывода из эксплуатации. Он включает требования, анализ, проектирование, реализацию, тестирование, внедрение, эксплуатацию, сопровождение и вывод из эксплуатации.

Семь групп процессов ISO/IEC 12207: процессы соглашения; организационного обеспечения проектов; управления проектом; технические процессы; процессы реализации ПО; поддержки ПО; повторного использования ПО. Процессы могут повторяться и выполняться параллельно: это не обязательная последовательность стадий.

**中文对应**

软件生命周期是从产生系统需求到系统退役的全过程，包括需求、分析、设计、实现、测试、部署、运行、维护和退役。

ISO/IEC 12207 的七个过程组：协议过程、组织项目保障过程、项目管理过程、技术过程、软件实现过程、软件支持过程、软件复用过程。过程可重复或并行执行，并非必须依次进行的阶段。

---

### 第2题（原题 23）：Требования. Иерархия требований. Примеры формулировок на разных уровнях.

**俄语答题 / Русский ответ**

Требование — условие или возможность, которой должна соответствовать система. Оно должно быть ясным, однозначным и проверяемым. Уровни: потребность бизнеса → возможность системы или пользователя → конкретное требование к ПО.

Пример: «Сократить ручную обработку заказов» → «Покупатель может оформить заказ самостоятельно» → «REQ-01: после подтверждения корзины система должна создать заказ с уникальным номером и показать этот номер покупателю». Нижний уровень уточняет верхний.

**中文对应**

需求是系统必须满足的条件或能力，应清晰、无歧义且可验证。层次为：业务需要→系统或用户能力→具体软件需求。

例：“减少人工处理订单”→“买家能够自行下单”→“REQ-01：确认购物车后，系统应创建具有唯一编号的订单，并向买家显示该编号。”下一层细化上一层。

**原图参考 / Иллюстрации**

![需求层次 / Иерархия требований](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/ierarch_requier.jpg)

---

### 第3题：Рецепты повышения производительности при высоком %IO wait. + Рецепты повышения производительности при высоком %User.

#### A（原题 78）：Рецепты повышения производительности при высоком %IO wait.

**俄语答题 / Русский ответ**

Высокий `%iowait` означает значительную долю простоя CPU при наличии незавершённого I/O; это сигнал для проверки, а не прямое измерение скорости диска.

Проверяют `iostat`, `vmstat` и активность процессов. Меры: уменьшить объём и частоту I/O, объединять запросы, использовать кэш и последовательный доступ; устранить подкачку; при подтверждённом ограничении устройства использовать более подходящее хранилище, например SSD. Выравнивание и размер блоков выбирают по устройству и нагрузке, а не по универсальному правилу.

**中文对应**

`%iowait` 高表示存在未完成 I/O 时，CPU 空闲时间比例较高。它是需要排查的信号，不是对磁盘速度的直接测量。

使用 `iostat`、`vmstat` 并查看进程活动。措施：减少 I/O 总量和频率，合并请求，使用缓存和顺序访问；消除换页压力；确认设备成为瓶颈后，采用更合适的存储，例如 SSD。块大小和对齐方式取决于设备及负载，没有统一规则。

**原图参考 / Иллюстрации**

![高 IO wait / Высокий %IO wait](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/high_io.png)

#### B（原题 80）：Рецепты повышения производительности при высоком %User.

**俄语答题 / Русский ответ**

Высокий `%User` означает, что CPU занят пользовательским кодом. Если производительность недостаточна, сначала профилируют приложение и находят горячие функции.

Меры: улучшить алгоритмы; убрать повторные вычисления и активное ожидание; сократить ненужные выделения и копирования; улучшить локальность данных и уменьшить cache/TLB misses; уменьшить конкуренцию потоков. Повторное использование объектов полезно, если оно снижает измеренные затраты. Затем рассматривают параллелизм, векторизацию, GPU или более мощный CPU и проверяют результат измерениями.

**中文对应**

`%User` 高表示 CPU 忙于用户态代码。性能不足时，先剖析应用并找到热点函数。

措施：改进算法；消除重复计算和忙等待；减少无必要的分配与复制；改善数据局部性，减少缓存／TLB 未命中；降低线程竞争。对象复用只有在确实降低开销时才有价值。之后再考虑并行、向量化、GPU 或更强 CPU，并通过测量确认结果。

**原图参考 / Иллюстрации**

![高 User / Высокий %User](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/high_user.png)

---

## Билет 2 · 变体 2

### 第1题（原题 2）：Модели ЖЦ (последовательная, инкрементная, эволюционная).

**俄语答题 / Русский ответ**

- Последовательная модель: стадии проходят последовательно; основные требования определяются заранее.
- Инкрементная модель: продукт поставляется частями; каждый инкремент добавляет готовые функции и проходит разработку и тестирование.
- Эволюционная модель: решение и требования уточняются по результатам предыдущих версий и обратной связи.

Главное различие: последовательное выполнение стадий, поэтапное добавление функций или постепенное уточнение решения.

**中文对应**

- 顺序模型：按阶段依次推进，主要需求预先确定。
- 增量模型：分批交付产品，每个增量增加可用功能，并经过开发和测试。
- 演化模型：根据前一版本的结果与反馈，逐步明确需求和改进方案。

主要区别是：依次完成阶段、分批增加功能，或逐步明确解决方案。

**原图参考 / Иллюстрации**

![生命周期模型总览 / Жизненный цикл и модели](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/LC_models.png)

---

### 第2题：Системы автоматической сборки: предпосылки появления. + Сервера сборки/непрерывной интеграции.

#### A（原题 41）：Системы автоматической сборки: предпосылки появления.

**俄语答题 / Русский ответ**

Автоматическая сборка появилась из-за трёх проблем:

1. Рутинные команды и сложные зависимости: нужны правильный порядок и воспроизводимость.
2. Различия среды разработки и целевой системы: архитектура, компиляторы, библиотеки и настройки.
3. Долгая сборка: нужны инкрементальная пересборка, параллелизм и сборки по расписанию.

Система сборки автоматически компилирует, связывает, тестирует и упаковывает продукт по описанию проекта, сокращая ручные ошибки.

**中文对应**

自动构建主要为解决三个问题而出现：

1. 命令重复、依赖复杂，需要保证执行顺序和可重复性。
2. 开发环境与目标系统的架构、编译器、库和配置不同。
3. 构建耗时，需要增量重建、并行执行和定时构建。

构建系统依据项目描述自动编译、链接、测试和打包，减少人工错误。

#### B（原题 51）：Сервера сборки/непрерывной интеграции.

**俄语答题 / Русский ответ**

Сервер непрерывной интеграции автоматически получает код, собирает проект и выполняет проверки после изменения репозитория или по расписанию. Типичная цепочка: checkout → сборка → тесты → анализ кода → сохранение артефактов и отчёт.

CI предполагает частую интеграцию небольших изменений и быструю обратную связь о сбоях. Сервер обеспечивает повторяемость проверок и доступность результатов. Примеры: Jenkins, GitLab CI, GitHub Actions.

**中文对应**

持续集成服务器在仓库更新或到达计划时间时，自动获取代码、构建并检查。典型流程：检出→构建→测试→代码分析→保存制品并生成报告。

CI 强调频繁集成小规模修改，并及时反馈失败。服务器保证检查可重复、结果可获取。例：Jenkins、GitLab CI、GitHub Actions。

**原图参考 / Иллюстрации**

![持续集成服务器 / Серверы CI](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/serv.png)

---

### 第3题（原题 69）：Мониторинг производительности: процессы.

**俄语答题 / Русский ответ**

Процессы наблюдают командами `top`, `ps`, `pidstat`. Проверяют CPU, память, состояние процесса, число потоков и переключения контекста. В `top`: PR — приоритет, VIRT — виртуальное адресное пространство, RES — резидентная память, SHR — разделяемая память.

Load average в Linux учитывает выполняемые/готовые к выполнению задачи и задачи в непрерываемом ожидании. Его сопоставляют с числом CPU и другими метриками. `pidstat -u -r -w 1` помогает найти процессы с высокой загрузкой, ошибками страниц и переключениями контекста.

**中文对应**

通过 `top`、`ps`、`pidstat` 监控进程，查看 CPU、内存、进程状态、线程数和上下文切换。`top` 中 PR 是优先级，VIRT 是虚拟地址空间，RES 是驻留内存，SHR 是共享内存。

Linux 的 load average 包含运行中／可运行任务和不可中断等待任务。应结合 CPU 数量及其他指标判断。`pidstat -u -r -w 1` 可定位 CPU 占用、缺页和上下文切换较高的进程。

---

## Билет 3 · 变体 3

### 第1题（原题 3）：Водопадная (каскадная) модель.

**俄语答题 / Русский ответ**

Водопадная модель организует разработку по стадиям: требования → анализ → проектирование → реализация → тестирование → внедрение. Результат каждой стадии документируется и служит основой следующей; возможны возвраты для исправления ошибок.

Плюсы: понятные этапы, удобство планирования и контроля. Минусы: работающий продукт появляется поздно, изменения на поздних стадиях дороги. Подходит для стабильных и понятных требований.

**中文对应**

瀑布模型依次进行：需求→分析→设计→实现→测试→部署。各阶段的成果形成文档，作为下一阶段的依据；发现错误时可以返回修改。

优点：阶段清晰，便于规划和管理。缺点：可运行产品出现较晚，后期修改成本高。适用于稳定、明确的需求。

**原图参考 / Иллюстрации**

![瀑布模型 / Каскадная модель](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/Cascade_model.png)

---

### 第2题（原题 42）：Системы сборки: Make и Makefile.

**俄语答题 / Русский ответ**

Make строит цели по правилам Makefile: `цель: зависимости`, затем команды с начальным TAB. Цель пересобирается, если отсутствует или хотя бы одна обычная зависимость новее неё.

Файл содержит правила, переменные, директивы и комментарии. `$@` — цель, `$<` — первая зависимость, `$^` — все зависимости без повторов, `$?` — зависимости новее цели. `.PHONY` объявляет цель без выходного файла, например `clean`. Пример правила: `main.o: main.c`, команда `$(CC) -c $< -o $@`. `make -f file` выбирает Makefile.

**中文对应**

Make 根据 Makefile 规则构建目标：`目标: 依赖`，下一行命令以 TAB 开头。目标不存在，或至少一个普通依赖比目标更新时，重新构建。

文件包含规则、变量、指令和注释。`$@` 是目标，`$<` 是首个依赖，`$^` 是去重后的全部依赖，`$?` 是比目标更新的依赖。`.PHONY` 声明不产生文件的伪目标，例如 `clean`。规则例：`main.o: main.c`，命令为 `$(CC) -c $< -o $@`。`make -f file` 指定 Makefile。

---

### 第3题：Системный анализ Linux "за 60 секунд". + Создание тестовой нагрузки и нагрузчики.

#### A（原题 73）：Системный анализ Linux "за 60 секунд".

**俄语答题 / Русский ответ**

Быстрая диагностика Linux:

1. `uptime` — load average за 1/5/15 минут.
2. `dmesg | tail` — ошибки устройств и памяти.
3. `vmstat 1` — очередь `r`, подкачка `si/so`, CPU `us/sy/wa`.
4. `mpstat -P ALL 1` — неравномерность загрузки CPU.
5. `pidstat 1` — процессы, создающие нагрузку.
6. `iostat -xz 1` — ожидание и загрузка I/O.
7. `free -m` — доступная память и swap.
8. `sar -n DEV 1` — трафик интерфейсов.
9. `sar -n TCP,ETCP 1` — соединения TCP и повторные передачи.
10. `top` — общая картина.

Вывод делают по сочетанию метрик и их изменению во времени.

**中文对应**

Linux 快速诊断：

1. `uptime`：1／5／15 分钟平均负载。
2. `dmesg | tail`：设备和内存错误。
3. `vmstat 1`：运行队列 `r`、换页 `si/so`、CPU 的 `us/sy/wa`。
4. `mpstat -P ALL 1`：各 CPU 负载是否不均。
5. `pidstat 1`：哪些进程产生负载。
6. `iostat -xz 1`：I/O 等待与负载。
7. `free -m`：可用内存和 swap。
8. `sar -n DEV 1`：网卡流量。
9. `sar -n TCP,ETCP 1`：TCP 连接与重传。
10. `top`：整体情况。

结合多个指标及其随时间的变化判断。

**原图参考 / Иллюстрации**

![vmstat 示例 1](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/vmstat_example_1.png)

![vmstat 示例 2](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/vmstat_example_2.png)

![vmstat 示例 3](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/vmstat_example_3.png)

![mpstat 示例](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/mpstat_example.jpg)

![iostat 示例](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/iostat_example.jpg)

![top 示例](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/top_example.png)

#### B（原题 74）：Создание тестовой нагрузки и нагрузчики.

**俄语答题 / Русский ответ**

Тестовая нагрузка должна отражать операции пользователей, интенсивность запросов, параллелизм, объём данных и длительность работы. Заранее задают ожидаемые метрики и критерии успешности.

Способы: синтетический генератор — гибко задаёт сценарии, но может отличаться от реальности; запись и воспроизведение реальной нагрузки — ближе к эксплуатации. Примеры нагрузчиков: JMeter и LoadRunner. Нагрузку повышают постепенно, учитывают прогрев и проверяют, что сам генератор не стал узким местом.

**中文对应**

测试负载应体现用户操作、请求速率、并发量、数据规模和运行时长，并提前确定预期指标和通过标准。

方法：合成负载生成器便于配置场景，但可能偏离真实情况；记录并重放实际负载更接近运行环境。工具例：JMeter、LoadRunner。逐步提升负载，考虑预热，并确认生成器本身没有成为瓶颈。

---

## Билет 4 · 变体 4

### 第1题（原题 4）：Методология Ройса.

**俄语答题 / Русский ответ**

Методология Ройса снижает риски последовательной разработки с помощью пяти мер:

1. Предварительное проектирование до детального анализа.
2. Полная и актуальная документация.
3. Предварительная пробная реализация сложных частей — «сделать дважды».
4. Планирование, контроль тестирования и участие независимых специалистов.
5. Участие заказчика в предварительном, критическом и окончательном рассмотрении проекта.

**中文对应**

罗伊斯方法通过五项措施降低顺序开发的风险：

1. 详细分析前先做初步设计。
2. 保持完整、及时更新的文档。
3. 对复杂部分先做试验实现，即“做两遍”。
4. 规划和管理测试，并让独立人员参与。
5. 客户参与初步、关键和最终项目评审。

**原图参考 / Иллюстрации**

![Royce 方法论 / Методология Ройса](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/Royce_modal.png)

---

### 第2题（原题 48）：Системы сборки: Gradle. Преимущества и файл сборки.

**俄语答题 / Русский ответ**

Gradle — система сборки с DSL на Groovy (`build.gradle`) или Kotlin (`build.gradle.kts`). Преимущества: гибкость задач и плагинов, поддержка разных языков, репозитории Maven/Ivy, инкрементальная сборка, кэширование и параллелизм.

Файл описывает плагины, репозитории, зависимости и задачи. Минимальный пример ниже подключает Java и зависимость для тестов. `gradle build` запускает сборку; при наличии Wrapper используют `./gradlew build`.

```groovy
plugins { id 'java' }
repositories { mavenCentral() }
dependencies { testImplementation 'junit:junit:4.13.2' }
```

**中文对应**

Gradle 使用 Groovy DSL（`build.gradle`）或 Kotlin DSL（`build.gradle.kts`）描述构建。优点：任务和插件灵活，支持多种语言及 Maven/Ivy 仓库，支持增量构建、缓存和并行执行。

构建文件描述插件、仓库、依赖和任务。上方最小例子启用 Java 插件并添加测试依赖。`gradle build` 执行构建；存在 Wrapper 时使用 `./gradlew build`。

上方代码依次对应：启用 Java、使用 Maven Central、引入 JUnit 测试依赖。

---

### 第3题：Профилирование приложений. Основные подходы. + Компромиссы (trade-offs) в производительности.

#### A（原题 75）：Профилирование приложений. Основные подходы.

**俄语答题 / Русский ответ**

Профилирование определяет, где приложение тратит CPU и память, какие методы наиболее дороги и где потоки ждут блокировки.

Подходы: инструментирование — добавление измерений в код, даёт подробные данные, но повышает накладные расходы; семплирование — периодическое получение стеков, даёт статистическую оценку с обычно меньшим вмешательством. Для памяти анализируют выделения, снимки кучи и удерживающие ссылки.

Профилируют под представительной нагрузкой, находят горячие точки, изменяют код и повторяют измерение.

**中文对应**

性能剖析确定应用在哪里消耗 CPU 和内存、哪些方法代价最大，以及线程在哪里等待锁。

方法：插桩向代码加入测量点，信息详细但开销较高；采样定期获取调用栈，提供统计估计，通常干扰较小。内存分析检查分配、堆快照和保留引用。

在有代表性的负载下剖析，找到热点，修改后再次测量。

#### B（原题 76）：Компромиссы (trade-offs) в производительности.

**俄语答题 / Русский ответ**

Оптимизация требует компромиссов между временем, памятью, ресурсами и сложностью.

Примеры: кэш ускоряет повторные запросы, но занимает память и требует обновления; индекс ускоряет поиск, но увеличивает размер данных и стоимость записи; сжатие уменьшает I/O, но расходует CPU; параллелизм повышает throughput, но добавляет синхронизацию.

Выбор зависит от узкого места и требований. Улучшение подтверждают измерениями на той же нагрузке, проверяя и побочные затраты.

**中文对应**

优化需要在时间、内存、资源和复杂度之间权衡。

例：缓存加快重复查询，但占内存且需要更新；索引加快查找，但增加数据体积和写入成本；压缩减少 I/O，但消耗 CPU；并行提高吞吐量，但增加同步开销。

选择取决于瓶颈和需求。应在相同负载下测量收益，同时检查额外成本。

---

## Билет 5 · 变体 5

### 第1题（原题 5）：Традиционная V-chart model J. Munson, B. Boehm.

**俄语答题 / Русский ответ**

V-модель связывает уровни разработки с уровнями тестирования. Слева уточняют требования и проектируют систему, внизу пишут код, справа проверяют результат.

Типичное соответствие: требования пользователя ↔ приёмочное тестирование; системные требования ↔ системное; архитектура ↔ интеграционное; детальный проект модулей ↔ модульное. Тесты и ожидаемые результаты планируют заранее. Верификация проверяет соответствие спецификации, валидация — потребностям пользователя.

**中文对应**

V 模型将开发层次与测试层次对应起来。左侧细化需求并设计系统，底部编码，右侧验证结果。

典型对应关系：用户需求↔验收测试；系统需求↔系统测试；架构↔集成测试；模块详细设计↔单元测试。测试及预期结果应提前规划。验证（verification）检查是否符合规格，确认（validation）检查是否满足用户需要。

**原图参考 / Иллюстрации**

![V 模型 / V-chart model](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/V-chart_modal.png)

---

### 第2题（原题 30）：Риски. Типы Рисков.

**俄语答题 / Русский ответ**

Риск — влияние неопределённости на цели проекта; для негативного риска оценивают вероятность события и величину потерь.

По управляемости риски бывают прямыми и непрямыми. По источнику: ресурсные — нехватка людей, денег или времени; бизнес-риски — конкуренция и нерентабельность; технические — технологии и зависимости; политические — конфликт интересов; форс-мажор — внешние чрезвычайные события. Пример: уход ключевого разработчика может задержать выпуск.

**中文对应**

风险是不确定性对项目目标的影响；对负面风险，评估事件发生概率和损失大小。

按可控性分为直接风险和间接风险。按来源分为：资源风险，如人员、资金或时间不足；业务风险，如竞争和亏损；技术风险，如技术方案和依赖问题；政治风险，如利益冲突；不可抗力，如外部突发事件。例：核心开发人员离职可能推迟发布。

---

### 第3题：Рецепты повышения производительности при высоком %SYS. + Рецепты повышения производительности при высоком %Idle.

#### A（原题 77）：Рецепты повышения производительности при высоком %SYS.

**俄语答题 / Русский ответ**

Высокий `%SYS` означает большую долю времени CPU в ядре. Проверяют системные вызовы, I/O, прерывания, переключения контекста и подкачку.

Меры: уменьшить число мелких вызовов чтения/записи, использовать буферизацию и пакетную обработку; сократить лишние потоки и переключения; устранить давление на память; найти избыточную сетевую или дисковую активность. Инструменты: `pidstat -w`, `vmstat`, `perf`, при необходимости `strace`. Оптимизируют выявленную причину, затем измеряют снова.

**中文对应**

`%SYS` 高表示 CPU 在内核态花费的时间比例高。检查系统调用、I/O、中断、上下文切换和换页。

措施：减少零碎读写调用，采用缓冲和批处理；减少多余线程和切换；缓解内存压力；定位过量网络或磁盘活动。工具有 `pidstat -w`、`vmstat`、`perf`，必要时使用 `strace`。针对查明的原因优化，再次测量。

**原图参考 / Иллюстрации**

![高 SYS 占比 / Высокий %SYS](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/high_sys.png)

#### B（原题 79）：Рецепты повышения производительности при высоком %Idle.

**俄语答题 / Русский ответ**

Высокий `%Idle` означает, что CPU имеет свободное время. Если скорость системы достаточна, исправление не требуется. Если приложение медленное, проверяют недостаток параллелизма, блокировки, ожидание сети или внешних сервисов.

Меры: распараллелить независимую работу, обоснованно изменить размер пула потоков, сократить критические секции, уменьшить синхронные ожидания. Перед увеличением числа потоков выясняют, чего именно они ждут. Сравнивают загрузку отдельных CPU: один занятый поток может ограничивать всю программу.

**中文对应**

`%Idle` 高表示 CPU 有空闲时间。系统速度已满足需求时无需处理。若应用仍慢，应检查并行度不足、锁、网络或外部服务等待。

措施：并行执行独立工作，合理调整线程池，缩短临界区，减少同步等待。增加线程前要明确线程在等什么。还应比较各 CPU：一个忙碌线程可能限制整个程序。

**原图参考 / Иллюстрации**

![高 Idle / Высокий %Idle](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/high_idle.png)

---

## Билет 6 · 变体 6

### 第1题（原题 6）：Многопроходная модель (Incremental model).

**俄语答题 / Русский ответ**

Инкрементная модель создаёт продукт последовательностью работающих приращений. Для каждого инкремента выполняются уточнение требований, проектирование, реализация, тестирование и интеграция. Части можно разрабатывать параллельно при согласованных интерфейсах.

Плюсы: ранняя поставка полезных функций, обратная связь, меньший объём изменений за один шаг. Минусы: сложность интеграции и оценки всего проекта; необходимы общая архитектура, стабильные интерфейсы и рефакторинг.

**中文对应**

增量模型通过一系列可运行的增量构建产品。每个增量都经历需求细化、设计、实现、测试和集成。接口明确后，不同部分可以并行开发。

优点：较早交付有用功能，及时获得反馈，每次修改规模较小。缺点：集成及整体估算较复杂，需要统一架构、稳定接口和重构。

**原图参考 / Иллюстрации**

![增量模型 / Incremental model](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/Incremental-model.png)

---

### 第2题（原题 34）：Системы контроля версий. Одновременная модификация файлов.

**俄语答题 / Русский ответ**

Система контроля версий хранит историю изменений, позволяет сравнивать и восстанавливать версии и поддерживает совместную работу. Типы: локальные (RCS), централизованные (SVN), распределённые (Git).

Одновременная работа организуется двумя способами: Lock–modify–unlock — файл блокируется на время изменения; Copy–modify–merge — разработчики меняют свои копии, затем объединяют результат. Несовместимые изменения вызывают конфликт, который нужно разрешить. Блокировка удобна для плохо сливаемых двоичных файлов, слияние — для текстового кода.

**中文对应**

版本控制系统保存修改历史，支持比较和恢复版本，帮助多人协作。类型包括本地式（RCS）、集中式（SVN）和分布式（Git）。

并发修改有两种方式：锁定—修改—解锁，修改期间锁住文件；复制—修改—合并，开发者修改各自副本后合并。不兼容的修改会产生冲突，需要解决。锁定适合难以合并的二进制文件，合并适合文本代码。

---

### 第3题（原题 70）：Мониторинг производительности: виртуальная память.

**俄语答题 / Русский ответ**

Виртуальная память отображает виртуальные страницы на физическую память; часть данных может находиться в swap или файлах. Minor page fault обслуживается без дискового I/O; major page fault требует I/O, например чтения файла или swap.

Проверяют доступную RAM, swap-in/swap-out, частоту major faults и сканирование страниц. Инструменты: `free`, `vmstat 1`, `sar -B`. Высокая интенсивность сканирования и подкачки может указывать на давление на память; одни только minor faults не доказывают её нехватку.

**中文对应**

虚拟内存把虚拟页映射到物理内存；部分数据可位于 swap 或文件中。Minor 缺页无需磁盘 I/O；major 缺页需要 I/O，例如读取文件或 swap。

应观察可用 RAM、换入／换出、major 缺页频率和页面扫描情况。工具有 `free`、`vmstat 1`、`sar -B`。频繁扫描与换页可能表示内存压力；仅有 minor 缺页不能证明内存不足。

**原图参考 / Иллюстрации**

![虚拟内存 / Виртуальная память](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/VM.png)

---

## Билет 7 · 变体 7

### 第1题（原题 7）：Модель прототипирования (80-е).

**俄语答题 / Русский ответ**

Прототипирование — создание упрощённой версии системы для уточнения требований и проверки решений.

Цикл: планирование → быстрый анализ → создание прототипа интерфейса, данных и функций → оценка пользователем → уточнение и новый прототип. После согласования создают окончательную систему, тестируют и внедряют её.

Плюс — ранняя обратная связь и снижение риска неверных требований. Риск — принять ограничения временного прототипа за готовое производственное решение.

**中文对应**

原型法通过构建简化版本来明确需求、验证方案。

流程：规划→快速分析→构建界面、数据和功能原型→用户评价→修改并形成新原型。确认后开发最终系统，进行测试和部署。

优点是尽早获得反馈，降低需求理解错误的风险。风险是把存在局限的临时原型误当作可直接投入生产的系统。

**原图参考 / Иллюстрации**

![原型模型 / Модель прототипирования](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/Prototype_model.png)

---

### 第2题：Системы сборки: Ant. Команды Ant. + Системы сборки: Ant-ivy.

#### A（原题 43）：Системы сборки: Ant. Команды Ant.

**俄语答题 / Русский ответ**

Ant — преимущественно императивная система сборки: разработчик задаёт действия в XML-файле `build.xml`. `project` содержит цели `target`; `depends` задаёт зависимости, внутри цели выполняются задачи (tasks).

Основные задачи: `mkdir`, `copy`, `delete`, `javac`, `jar`, `java`, `junit`, `echo`. Свойства задаются через `property` и используются как `${name}`. Запуск: `ant` — цель по умолчанию, `ant test` — указанная цель, `ant -f other.xml` — другой файл. `antcall` вызывает цель того же проекта, `ant` — сборку другого файла.

**中文对应**

Ant 主要采用命令式构建，在 XML 文件 `build.xml` 中声明具体动作。`project` 包含多个 `target`，`depends` 指定目标依赖，目标内部执行任务（tasks）。

主要任务：`mkdir`、`copy`、`delete`、`javac`、`jar`、`java`、`junit`、`echo`。通过 `property` 设置属性，用 `${name}` 引用。`ant` 执行默认目标，`ant test` 执行指定目标，`ant -f other.xml` 指定其他文件。`antcall` 调用同一项目的目标，`ant` 任务调用其他文件的构建。

#### B（原题 44）：Системы сборки: Ant-ivy.

**俄语答题 / Русский ответ**

Apache Ivy — менеджер зависимостей, интегрируемый с Ant. Ant выполняет сборку, Ivy находит и загружает нужные библиотеки и их транзитивные зависимости.

В `ivy.xml` указывают модуль и зависимости, в настройках — репозитории. Задачи Ivy вызываются из `build.xml`: resolve разрешает зависимости, retrieve копирует артефакты в нужное место. Поддерживаются репозитории Maven. Польза — отсутствие ручного скачивания и управления наборами библиотек.

**中文对应**

Apache Ivy 是可与 Ant 集成的依赖管理器。Ant 执行构建，Ivy 查找和下载所需库及其传递依赖。

`ivy.xml` 声明模块和依赖，配置中指定仓库。通过 `build.xml` 调用 Ivy 任务：resolve 解析依赖，retrieve 将制品复制到指定位置。支持 Maven 仓库。作用是减少手动下载和管理库文件的工作。

---

### 第3题（原题 60）：Интеграционное тестирование. Стратегии интеграции.

**俄语答题 / Русский ответ**

Интеграционное тестирование проверяет взаимодействие компонентов или систем: интерфейсы, вызовы, протоколы и передачу данных.

Стратегии: сверху вниз — от управляющих модулей, нижние заменяются заглушками; снизу вверх — от низкоуровнеeвых модулей, верхние заменяются драйверами; функциональная — по пользовательским сценариям; ядро — сначала минимальный работающий набор; большой взрыв — объединение всех компонентов сразу.

Поэтапная интеграция облегчает поиск причины ошибки. При большом взрыве локализовать дефект сложнее.

**中文对应**

集成测试检查组件或系统之间的交互，包括接口、调用、协议和数据传递。

策略：自顶向下，从控制模块开始，用桩代替底层；自底向上，从底层模块开始，用驱动代替上层；按功能，逐个集成用户场景；核心法，先集成最小可运行集合；大爆炸，一次合并全部组件。

逐步集成有利于定位错误；大爆炸方式较难找出缺陷来源。

---

## Билет 8 · 变体 8

### 第1题（原题 8）：RAD методология.

**俄语答题 / Русский ответ**

RAD (Rapid Application Development) — быстрая разработка с прототипами, повторным использованием компонентов и активным участием пользователя.

Фазы: планирование требований → пользовательское проектирование моделей и прототипов → конструирование системы → переход к эксплуатации: тестирование, перенос данных и обучение. Обратная связь поступает в ходе разработки. Подход удобен для модульных бизнес-приложений при доступности заказчика.

**中文对应**

RAD（快速应用开发）利用原型、组件复用和用户积极参与来加快开发。

阶段：需求规划→用户参与模型和原型设计→系统构建→转入运行，包括测试、数据迁移和培训。开发中持续获取反馈。适合能及时获得客户参与的模块化业务应用。

**原图参考 / Иллюстрации**

![RAD 模型 / RAD model](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/RAD_model.png)

---

### 第2题：Свойства и типы требований (FURPS+). + Атрибуты требований.

#### A（原题 24）：Свойства и типы требований (FURPS+).

**俄语答题 / Русский ответ**

Свойства хороших требований: корректность, однозначность, полнота, непротиворечивость, приоритизация, проверяемость, модифицируемость и отслеживаемость.

FURPS+: F — Functionality, функции; U — Usability, удобство использования; R — Reliability, надёжность; P — Performance, производительность; S — Supportability, поддерживаемость. «+» включает ограничения проектирования, реализации, интерфейсов и физические ограничения. F описывает функции, остальные группы — качества и ограничения системы.

**中文对应**

良好需求的属性：正确、无歧义、完整、一致、有优先级、可验证、可修改、可追踪。

FURPS+：F 为功能；U 为易用性；R 为可靠性；P 为性能；S 为可支持性。“+”包括设计、实现、接口和物理约束。F 描述系统功能，其余组描述系统质量和限制。

#### B（原题 28）：Атрибуты требований.

**俄语答题 / Русский ответ**

Атрибуты требований — данные для управления ими: ID, приоритет, статус, трудоёмкость, риск, стабильность и целевая версия.

Приоритет MoSCoW: Must — обязательно; Should — важно; Could — желательно; Won't — не входит в текущую поставку. Статусы: предложено, одобрено, реализовано, проверено или отклонено. Трудоёмкость оценивают, например, в человеко-часах; стабильность показывает вероятность изменения требования.

**中文对应**

需求属性是用于管理需求的信息，包括编号、优先级、状态、工作量、风险、稳定性和目标版本。

MoSCoW 优先级：Must 必须有；Should 重要；Could 可以有；Won't 本次不交付。状态包括提出、批准、实现、验证或拒绝。工作量可用人时估算；稳定性表示需求发生变化的可能性。

---

### 第3题：GIT: Организация ветвей репозитория. + GIT: Плагин git-flow.

#### A（原题 39）：GIT: Организация ветвей репозитория.

**俄语答题 / Русский ответ**

В модели Git Flow ветви имеют разные назначения:

- `master/main` — выпущенные стабильные версии; `develop` — интеграция следующей версии.
- `feature` создаётся от `develop`, реализует функцию и сливается обратно.
- `release` создаётся от `develop` для подготовки выпуска; результат сливается в `master/main` и `develop`, выпуск отмечается тегом.
- `hotfix` создаётся от `master/main` для срочного исправления; результат возвращается в `master/main` и `develop`.

Это соглашение о работе команды, а не обязательное устройство любого Git-проекта.

**中文对应**

Git Flow 模型中各类分支承担不同任务：

- `master/main` 保存已发布的稳定版本；`develop` 集成下一版本。
- `feature` 从 `develop` 创建，完成功能后合回。
- `release` 从 `develop` 创建，用于发布准备；完成后合入 `master/main` 与 `develop`，并为发布打标签。
- `hotfix` 从 `master/main` 创建，用于紧急修复；完成后合入 `master/main` 与 `develop`。

这是团队协作约定，并非所有 Git 项目必须采用的结构。

**原图参考 / Иллюстрации**

![Git 命令示意 / Команды Git](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/command.png)

#### B（原题 40）：GIT: Плагин git-flow.

**俄语答题 / Русский ответ**

git-flow — расширение Git, автоматизирующее операции модели Git Flow. `git flow init` задаёт основные ветви и префиксы.

Типичный цикл: `git flow feature start login` → изменения и обычные commits → `git flow feature finish login`. Start создаёт ветвь функции и переключается на неё; finish объединяет её с `develop`. Аналогично существуют `release start/finish` и `hotfix start/finish`. Расширение упрощает команды, но конфликты и тестирование остаются задачей разработчика.

**中文对应**

git-flow 是自动执行 Git Flow 分支操作的 Git 扩展。`git flow init` 配置主要分支及前缀。

典型流程：`git flow feature start login`→修改并正常提交→`git flow feature finish login`。Start 创建并切换到功能分支；finish 将其合入 `develop`。同样存在 `release start/finish` 和 `hotfix start/finish`。扩展简化操作，但开发者仍须解决冲突并测试。

**原图参考 / Иллюстрации**

![Git Flow / Git Flow](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/flow.png)

---

## Билет 9 · 变体 9

### 第1题（原题 9）：Спиральная модель.

**俄语答题 / Русский ответ**

Спиральная модель — итеративная разработка, управляемая рисками. Каждый виток включает:

1. Определение целей, альтернатив и ограничений.
2. Анализ рисков и их снижение, например прототипированием.
3. Разработку и проверку следующей версии.
4. Оценку результатов и планирование следующего витка.

Плюс — ранняя работа с рисками; минус — сложность управления и необходимость квалифицированной оценки рисков. Подходит для крупных и неопределённых проектов.

**中文对应**

螺旋模型是风险驱动的迭代开发。每一圈包括：

1. 确定目标、备选方案和约束。
2. 分析并降低风险，例如制作原型。
3. 开发和验证下一版本。
4. 评估结果并规划下一轮。

优点是尽早处理风险；缺点是管理复杂，需要专业的风险评估能力。适用于大型、不确定性高的项目。

**原图参考 / Иллюстрации**

![螺旋模型 / Спиральная модель](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/Spiral_model.png)

---

### 第2题（原题 26）：Требования к удобству использования и надежности.

**俄语答题 / Русский ответ**

Удобство использования: учёт особенностей пользователя, эстетика, согласованность интерфейса, контекстная справка, мастера, пользовательская документация и обучение. Пример: новый пользователь должен оформить заказ без помощи за 3 минуты.

Надёжность: частота и тяжесть отказов, восстанавливаемость, предсказуемость, точность и средняя наработка между отказами (MTBF). Пример: при восстановлении после сбоя подтверждённые заказы не теряются.

**中文对应**

易用性包括用户特点、美观、界面一致性、上下文帮助、向导、用户文档和培训。例：新用户应能在无人帮助下于 3 分钟内完成下单。

可靠性包括故障频率和严重程度、可恢复性、可预测性、准确性及平均故障间运行时间（MTBF）。例：故障恢复后，已确认的订单不会丢失。

---

### 第3题（原题 64）：Тестирование системы в целом. Альфа- и бета-тестирование. Приемочное тестирование.

**俄语答题 / Русский ответ**

Альфа-тестирование проводится в среде разработчика с участием представителей пользователей или независимых тестировщиков. Бета-тестирование выполняют внешние пользователи в реальных условиях, передавая отзывы и сведения о дефектах.

Приёмочное тестирование проверяет согласованные критерии и определяет, можно ли принять систему и использовать её. Заказчик или уполномоченные пользователи оценивают соответствие своим потребностям. Главное различие — участники, среда и цель: получение обратной связи или решение о приёмке.

**中文对应**

Alpha 测试在开发方环境中进行，由用户代表或独立测试人员参与。Beta 测试由外部用户在真实环境中进行，反馈使用意见和缺陷。

验收测试检查约定的验收标准，判断系统是否可以被接受并投入使用。客户或授权用户评估是否满足自身需要。主要区别在于参与者、环境和目的：获取反馈，或决定是否验收。

---

## Билет 10 · 变体 10

### 第1题（原题 15）：*UP методологии (90-е). RUP: основы процесса.

**俄语答题 / Русский ответ**

UP — семейство унифицированных процессов; RUP — Rational Unified Process. Процесс итеративный и инкрементный, ориентирован на прецеденты, архитектуру и снижение рисков.

Фазы: Начало → Проектирование → Построение → Внедрение. Дисциплины: бизнес-моделирование, требования, анализ и проектирование, реализация, тестирование, развёртывание, управление изменениями и конфигурацией, проектом и средой разработки. Дисциплины повторяются в разных фазах с разной интенсивностью.

Роль — обязанности; деятельность — выполняемая работа; артефакт — её результат. Фаза заканчивается вехой: оценкой готовности двигаться дальше.

**中文对应**

UP 是统一过程家族，RUP 是 Rational 统一过程。它采用迭代和增量开发，以用例、架构和降低风险为重点。

阶段：初始→细化设计→构建→交付。规程：业务建模、需求、分析与设计、实现、测试、部署，以及变更与配置管理、项目管理和开发环境管理。各规程会在不同阶段以不同强度重复开展。

角色是职责，活动是执行的工作，制品是工作成果。每个阶段以里程碑结束，判断是否可以进入下一阶段。

**原图参考 / Иллюстрации**

![RUP 阶段 / Стадии RUP](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/rup_stadii.png)

---

### 第2题（原题 27）：Требования к производительности и поддерживаемости.

**俄语答题 / Русский ответ**

Производительность: скорость, эффективность, доступность, пропускная способность, время отклика и восстановления, использование ресурсов. Требуются численные условия: «При 100 одновременных пользователях 95% запросов выполняются не дольше 2 секунд».

Поддерживаемость: расширяемость, адаптируемость, сопровождаемость, совместимость, конфигурируемость, обслуживаемость, устанавливаемость и локализуемость. Пример: язык интерфейса меняется настройкой без изменения кода.

**中文对应**

性能包括速度、效率、可用性、吞吐量、响应和恢复时间、资源使用量。应给出量化条件，例如：“100 名用户并发时，95% 的请求在 2 秒内完成。”

可支持性包括可扩展性、适应性、可维护性、兼容性、可配置性、可服务性、可安装性和可本地化性。例：通过配置切换界面语言，无需修改代码。

---

### 第3题（原题 63）：Тестирование системы в целом. Системное тестирование. Тестирование производительности.

**俄语答题 / Русский ответ**

Системное тестирование проверяет интегрированную систему целиком относительно требований: функции, надёжность, совместимость и работу под нагрузкой.

Характеристики CARAT: Capacity — допустимый объём данных/нагрузки; Accuracy — точность; Response Time — время ответа; Availability — доступность; Throughput — число операций за единицу времени. Доступность за период: `A = время работы / (время работы + простой)`.

Нагрузочный тест проверяет ожидаемую нагрузку, стресс-тест — поведение за её пределами. Инструменты: JMeter, LoadRunner. Результаты сравнивают с заданными порогами.

**中文对应**

系统测试根据需求检查集成后的整个系统，包括功能、可靠性、兼容性和负载下的表现。

CARAT 特征：Capacity 为可承受的数据量或负载；Accuracy 为准确性；Response Time 为响应时间；Availability 为可用性；Throughput 为单位时间操作数。某时段的可用性：`A = 运行时间 /（运行时间 + 停机时间）`。

负载测试检查预期负载，压力测试检查超出该范围后的行为。工具有 JMeter、LoadRunner。结果应与事先规定的阈值比较。

---

## Билет 11 · 变体 11

### 第1题：RUP: Фаза «Начало». + RUP: Фаза «Проектирование».

#### A（原题 16）：RUP: Фаза «Начало».

**俄语答题 / Русский ответ**

Фаза «Начало» (Inception) определяет, зачем нужен проект и стоит ли его выполнять.

Цели: согласовать границы и основные требования; определить ключевые прецеденты; предложить возможное решение; оценить стоимость, сроки и риски; подготовить окружение.

Веха Lifecycle Objectives: заинтересованные стороны согласны с целями, объёмом, оценками и основными рисками; принимается решение о продолжении проекта.

**中文对应**

初始阶段（Inception）明确项目为何需要开展、是否值得开展。

目标：确定范围和主要需求，识别关键用例，提出候选方案，估算成本、工期和风险，准备环境。

里程碑 Lifecycle Objectives：各方认可项目目标、范围、估算和主要风险，决定是否继续。

#### B（原题 17）：RUP: Фаза «Проектирование».

**俄语答题 / Русский ответ**

Фаза «Проектирование» (Elaboration) создаёт устойчивую основу для реализации.

Цели: уточнить требования; зафиксировать базовую архитектуру; проверить её исполняемым прототипом; снизить главные технические риски; уточнить план, сроки и стоимость построения.

Веха Lifecycle Architecture: архитектура проверена, ключевые риски снижены, план реализации реалистичен и согласован.

**中文对应**

细化设计阶段（Elaboration）为实现建立稳定基础。

目标：细化需求，确定基线架构，用可执行原型验证架构，降低主要技术风险，细化构建阶段的计划、工期和成本。

里程碑 Lifecycle Architecture：架构得到验证，关键风险已降低，实现计划可行并获得认可。

---

### 第2题（原题 33）：Изменение. Общая модель управления изменениями.

**俄语答题 / Русский ответ**

Изменение — модификация требований, кода, документации или других артефактов. Управление изменениями делает её контролируемой и прослеживаемой.

Процесс: зарегистрировать Change Request → оценить влияние, техническую осуществимость, стоимость и пользу → одобрить, отклонить или отложить → спланировать и реализовать → протестировать, обновить документацию и выпустить → проверить и закрыть запрос. Решение принимает уполномоченный руководитель или комитет по изменениям. В журнале сохраняются ID, автор, дата, описание и статус.

**中文对应**

变更是对需求、代码、文档或其他制品的修改。变更管理使修改可控、可追踪。

流程：登记变更请求→分析影响、技术可行性、成本和收益→批准、拒绝或推迟→规划并实现→测试、更新文档和发布→验证并关闭请求。由获授权的负责人或变更控制委员会决定。日志保留编号、提出者、日期、描述和状态。

**原图参考 / Иллюстрации**

![变更管理模型 / Модель управления изменениями](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/changes_model.jpeg)

---

### 第3题：Основные понятия тестирования. Цели тестирования. + Понятие полного тестового покрытия и его достижимость. Пример.

#### A（原题 52）：Основные понятия тестирования. Цели тестирования.

**俄语答题 / Русский ответ**

Тестирование — деятельность по оценке качества ПО и поиску дефектов. Термины: mistake/error — человеческая ошибка; fault/defect — дефект артефакта; failure — наблюдаемый отказ при выполнении; bug — разговорное название дефекта. В описании состояния системы error также может означать отклонение фактического результата от правильного.

Цели: обнаружить и предупредить дефекты, повысить уверенность в качестве, дать информацию для решений. Успешные тесты показывают соответствие проверенным требованиям, но не доказывают отсутствие всех дефектов.

**中文对应**

测试是评估软件质量、寻找缺陷的活动。术语：mistake/error 可指人的错误；fault/defect 指制品中的缺陷；failure 指运行时可观察的失效；bug 是缺陷的口语称呼。描述系统状态时，error 也可指实际结果与正确结果的偏差。

目标：发现和预防缺陷，提高质量信心，为决策提供信息。测试通过表明符合已检查的要求，但不能证明不存在任何缺陷。

#### B（原题 53）：Понятие полного тестового покрытия и его достижимость. Пример.

**俄语答题 / Русский ответ**

Тестовое покрытие — доля проверенных элементов выбранного критерия: требований, операторов, ветвей и т. п. 100% покрытия операторов достижимо, но не означает проверку всех входов и сценариев.

Пример исчерпывающего тестирования: два 32-битных аргумента дают `2^64` комбинаций. При одной проверке за такт на 3 ГГц перебор занял бы около 200 лет. Поэтому выбирают представительные и граничные значения, учитывают риски и явно задают критерий покрытия. Ожидаемый результат должен определяться независимо от проверяемой реализации.

**中文对应**

测试覆盖率是选定标准中已检查元素的比例，例如需求、语句或分支。100% 语句覆盖可以达到，但不等于检查了所有输入和场景。

穷举例子：两个 32 位参数产生 `2^64` 种组合。若 3 GHz 处理器每周期检查一次，穷举约需 200 年。因此应选择代表值和边界值，考虑风险，并明确覆盖标准。预期结果必须独立于被测实现确定。

---

## Билет 12 · 变体 12

### 第1题：RUP: Фаза «Построение». + RUP: Фаза «Внедрение».

#### A（原题 18）：RUP: Фаза «Построение».

**俄语答题 / Русский ответ**

Фаза «Построение» (Construction) создаёт работоспособный продукт на основе согласованной архитектуры.

Выполняются анализ оставшихся требований, проектирование, кодирование, интеграция и тестирование. Цель — нужная функциональность и качество в пределах бюджета и сроков; архитектурные изменения минимальны. Готовятся документация и материалы для внедрения.

Веха Initial Operational Capability: продукт достаточно стабилен, чтобы начать передачу пользователям.

**中文对应**

构建阶段（Construction）基于已确定的架构开发可运行产品。

开展剩余需求分析、设计、编码、集成和测试。目标是在预算和工期内实现所需功能与质量，尽量减少架构变更，并准备部署所需文档和材料。

里程碑 Initial Operational Capability：产品已足够稳定，可以开始交付给用户。

#### B（原题 19）：RUP: Фаза «Внедрение».

**俄语答题 / Русский ответ**

Фаза «Внедрение» (Transition) передаёт продукт пользователям.

Работы: бета-тестирование, исправление дефектов и проблем производительности, установка и перенос данных, обучение пользователей, подготовка поддержки и выпуска. Проверяется соответствие продукта исходным целям.

Веха Product Release: пользователи принимают продукт, он готов к самостоятельной эксплуатации; оцениваются итоговые затраты.

**中文对应**

交付阶段（Transition）将产品交给用户。

工作：Beta 测试、修复缺陷和性能问题、安装与数据迁移、用户培训、准备支持和正式发布，并检查产品是否符合最初目标。

里程碑 Product Release：用户接受产品，能够独立使用，并评估最终成本。

---

### 第2题（原题 35）：Subversion. Архитектура системы и репозиторий.

**俄语答题 / Русский ответ**

Subversion — централизованная система контроля версий: сервер хранит репозиторий, клиент работает с рабочей копией. Репозиторий доступен через `svn://` и `svn+ssh://` с svnserve, через HTTP(S) с Apache или локально через `file://`.

Каждый успешный commit создаёт новую глобальную ревизию репозитория. Принятая структура каталогов: `trunk` — основная разработка, `branches` — ветви, `tags` — зафиксированные версии. В SVN ветви и теги создаются копированием каталогов.

**中文对应**

Subversion 是集中式版本控制系统：服务器保存仓库，客户端操作工作副本。通过 svnserve 使用 `svn://`、`svn+ssh://`，通过 Apache 使用 HTTP(S)，也可用 `file://` 访问本地仓库。

每次成功提交都会生成新的仓库全局修订号。常用目录：`trunk` 为主线，`branches` 为分支，`tags` 为固定版本。SVN 通过复制目录创建分支和标签。

**原图参考 / Иллюстрации**

![SVN 架构 / Архитектура SVN](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/svn.png)

![SVN 仓库 / Репозиторий SVN](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/rep_svn.png)

---

### 第3题：Ключевые характеристики производительности. + Нисходящий метод поиска узких мест.

#### A（原题 66）：Ключевые характеристики производительности.

**俄语答题 / Русский ответ**

Основные характеристики:

- Время отклика/latency — задержка от запроса до оговорённого результата; границы измерения должны быть заданы.
- Throughput — число завершённых операций за единицу времени.
- Utilization — доля времени занятости ресурса; очередь показывает накопление ожидающей работы.
- Насыщение — рост нагрузки перестаёт давать сопоставимый рост throughput, а задержки растут.
- Масштабируемость — изменение производительности при добавлении ресурсов; эффективность — результат относительно затрат ресурсов.

Метрики оценивают вместе при известной нагрузке.

**中文对应**

主要特征：

- 响应时间／latency：从请求到约定结果的延迟，必须明确测量起止点。
- Throughput：单位时间完成的操作数。
- Utilization：资源忙碌时间比例；队列体现等待任务的积累。
- 饱和：负载增加不再带来相应吞吐量增长，而延迟上升。
- 可扩展性：增加资源后性能如何变化；效率：产出相对于资源消耗的水平。

应在已知负载下结合这些指标判断。

#### B（原题 67）：Нисходящий метод поиска узких мест.

**俄语答题 / Русский ответ**

Нисходящий поиск узкого места идёт от системы к деталям:

1. Проверить аппаратуру, конфигурацию, журналы и ошибки.
2. Измерить CPU, память, диски, сеть и очереди средствами ОС.
3. Найти проблемный процесс и исследовать алгоритмы, I/O, потоки и блокировки профилировщиком.
4. При необходимости исследовать кэши, TLB, инструкции и предсказание переходов.

Узкое место ограничивает производительность всей системы. Исправляют подтверждённую причину и повторяют измерения с той же нагрузкой.

**中文对应**

自顶向下定位瓶颈，从整体进入细节：

1. 检查硬件、配置、日志和错误。
2. 使用操作系统工具测量 CPU、内存、磁盘、网络和队列。
3. 找到问题进程，用性能分析器检查算法、I/O、线程和锁。
4. 必要时继续检查缓存、TLB、指令及分支预测。

瓶颈限制整个系统的性能。应修复有证据支持的原因，再用相同负载测量。

---

## Билет 13 · 变体 13

### 第1题（原题 20）：Манифест Agile (2001).

**俄语答题 / Русский ответ**

Манифест Agile (2001) формулирует четыре ценности:

1. Люди и взаимодействие важнее процессов и инструментов.
2. Работающий продукт важнее исчерпывающей документации.
3. Сотрудничество с заказчиком важнее согласования условий контракта.
4. Готовность к изменениям важнее следования первоначальному плану.

То, что справа, тоже важно, но то, что слева, ценится больше. Цель — регулярно поставлять полезное ПО и учитывать обратную связь.

**中文对应**

敏捷宣言（2001）包含四项价值观：

1. 个体与互动高于流程和工具。
2. 可工作的产品高于详尽文档。
3. 客户合作高于合同谈判。
4. 响应变化高于遵循原定计划。

右侧内容也有价值，但更重视左侧。目标是定期交付有用软件，并根据反馈调整。

---

### 第2题（原题 49）：Системы сборки: GNU autotools. Создание конфигурации проекта.

**俄语答题 / Русский ответ**

GNU Autotools создаёт переносимую систему конфигурации и сборки. Разработчик готовит `configure.ac` — проверки среды, и `Makefile.am` — описание сборки. `autoscan` может создать исходный шаблон `configure.scan`.

`aclocal` собирает макросы в `aclocal.m4`; `autoconf` создаёт `configure`; `autoheader` — `config.h.in`, если он нужен; `automake` — `Makefile.in`. Обычно необходимые инструменты запускает `autoreconf -i`. Полученные файлы включают в исходный дистрибутив для последующей настройки на машине пользователя.

**中文对应**

GNU Autotools 创建可移植的配置和构建系统。开发者编写 `configure.ac` 描述环境检查，编写 `Makefile.am` 描述构建。`autoscan` 可生成初始模板 `configure.scan`。

`aclocal` 将宏汇集到 `aclocal.m4`；`autoconf` 生成 `configure`；需要配置头文件时，`autoheader` 生成 `config.h.in`；`automake` 生成 `Makefile.in`。通常用 `autoreconf -i` 调用所需工具。生成文件随源码包发布，供用户机器进行后续配置。

**原图参考 / Иллюстрации**

![GNU Autotools 工具链 / Инструменты GNU Autotools](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/tools.png)

---

### 第3题（原题 71）：Мониторинг производительности: буферизированный файловый ввод-вывод.

**俄语答题 / Русский ответ**

При буферизированном файловом I/O данные проходят через кэш ОС. Чтение может обслуживаться из кэша; запись изменяет страницы в памяти, которые позднее записываются на устройство. Это уменьшает число обращений к диску, но требует памяти.

`iostat -x 1`: `r/s`, `w/s` — операций в секунду; `rkB/s`, `wkB/s` — объём передачи; `await` — среднее время запроса, включая очередь; `%util` — доля времени занятости устройства. Рост ожидания и очереди указывает на возможное ограничение I/O. Случайный доступ особенно дорог на HDD.

**中文对应**

缓冲文件 I/O 经过操作系统缓存。读取可直接命中缓存；写入先修改内存页，再写回设备。这减少磁盘访问，但消耗内存。

`iostat -x 1`：`r/s`、`w/s` 是每秒操作数；`rkB/s`、`wkB/s` 是传输量；`await` 是包含排队时间的平均请求耗时；`%util` 是设备忙碌时间比例。等待和队列增长可能表明 I/O 成为限制。随机访问在 HDD 上尤其昂贵。

**原图参考 / Иллюстрации**

![缓冲文件 I/O / Буферизированный файловый ввод-вывод](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/file_buffered_io.png)

---

## Билет 14 · 变体 14

### 第1题（原题 21）：Scrum.

**俄语答题 / Русский ответ**

Scrum — фреймворк итеративной разработки. Спринт длится не более месяца и создаёт пригодный к использованию инкремент, соответствующий Definition of Done.

Ответственности: Product Owner упорядочивает Product Backlog и отвечает за ценность; Scrum Master помогает применять Scrum и устранять препятствия; Developers создают инкремент.

Артефакты: Product Backlog, Sprint Backlog, Increment. События: Sprint Planning, Daily Scrum, Sprint Review, Sprint Retrospective. Review оценивает продукт и дальнейшие шаги; Retrospective улучшает работу команды и проводится каждый спринт.

**中文对应**

Scrum 是迭代开发框架。Sprint 不超过一个月，产出符合完成定义（Definition of Done）的可用增量。

职责：Product Owner 排序产品待办列表并对价值负责；Scrum Master 帮助团队应用 Scrum、消除障碍；Developers 开发增量。

制品：Product Backlog、Sprint Backlog、Increment。事件：迭代规划、每日站会、迭代评审、迭代回顾。评审关注产品和下一步工作；回顾关注团队工作方式，每个 Sprint 都开展。

**原图参考 / Иллюстрации**

![Scrum 流程 / Механика Scrum](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/МЕХАНИКА-СКРАМ-ПРОЦЕССА.jpg)

---

### 第2题（原题 31）：Управления рисками. Деятельности, связанные с оценкой.

**俄语答题 / Русский ответ**

Оценка риска включает идентификацию, классификацию, анализ вероятности и последствий, затем приоритизацию. Источники: разработка продукта, среда разработки и ограничения проекта. Используют экспертные оценки, модели, шкалы и матрицу «вероятность × ущерб».

Экспозиция риска: `RE = P × L`, где P — вероятность, L — потери. Например, `0,2 × 100 000 = 20 000`. Результат — реестр рисков и список наиболее важных рисков, например TOP-10.

**中文对应**

风险评估包括识别、分类、分析概率和后果，再确定优先级。风险可来自产品开发、开发环境和项目约束。采用专家评估、模型、等级量表和“概率×损失”矩阵。

风险暴露值：`RE = P × L`，P 是概率，L 是损失。例如 `0.2 × 100 000 = 20 000`。结果形成风险登记表及重点风险列表，例如 TOP-10。

**原图参考 / Иллюстрации**

![软件开发风险 / Риски разработки ПО](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/software_development_risk.png)

---

### 第3题：Понятие тестового случая и сценария. + Выбор тестового покрытия и количества тестов. Анализ эквивалентности.

#### A（原题 57）：Понятие тестового случая и сценария.

**俄语答题 / Русский ответ**

Тестовый случай задаёт предусловия, входные данные, действия и ожидаемый результат. Тестовый сценарий объединяет связанные случаи или шаги для проверки пользовательского пути.

Пример случая: карта активна, баланс 1000, комиссия отсутствует; ввести правильный PIN и снять 200; ожидать выдачу 200 и баланс 800. Сценарий: вставить карту → ввести PIN → выбрать снятие → получить деньги → забрать карту. Нужен и отрицательный случай: неверный PIN → отказ в доступе и сообщение об ошибке.

**中文对应**

测试用例包含前置条件、输入数据、操作和预期结果。测试场景将相关用例或步骤组合起来，检查一条用户路径。

用例：卡有效、余额 1000、无手续费；输入正确 PIN 并取款 200；预期吐出 200，余额变为 800。场景：插卡→输入 PIN→选择取款→取钱→取卡。还需要负向用例：PIN 错误→拒绝访问并提示错误。

#### B（原题 58）：Выбор тестового покрытия и количества тестов. Анализ эквивалентности.

**俄语答题 / Русский ответ**

Количество тестов выбирают по рискам, требованиям, критерию покрытия и доступным ресурсам. Методы: эквивалентное разбиение, граничные значения, таблицы решений, переходы состояний и пользовательские сценарии.

Эквивалентное разбиение делит данные на классы с одинаковым ожидаемым поведением; из каждого берут представителя. Пример: допустимый целый возраст 18–60. Классы: меньше 18, 18–60, больше 60; представители: 10, 30, 70. Границы: 17, 18, 19, 59, 60, 61. Проверяются и допустимые, и недопустимые классы.

**中文对应**

测试数量由风险、需求、覆盖标准和资源决定。方法包括等价类划分、边界值、决策表、状态转换和用户场景。

等价类划分把输入分为预期行为相同的类别，从每类选代表值。例如允许的整数年龄为 18–60 岁。类别：小于 18、18–60、大于 60；代表值：10、30、70。边界值：17、18、19、59、60、61。有效类与无效类都要检查。

---

## Билет 15 · 变体 15

### 第1题（原题 22）：Disciplined Agile 2.X (2013).

**俄语答题 / Русский ответ**

Disciplined Agile расширяет гибкую разработку до полного жизненного цикла и уровня организации. Он сочетает идеи Agile, Lean и UP и позволяет выбирать процесс под контекст проекта.

В рассматриваемом жизненном цикле DAD три фазы: Начало, Построение, Внедрение. Помимо разработки учитываются архитектура, повторное использование, управление персоналом и портфелем, эксплуатация, поддержка и непрерывное улучшение процессов. Цель — согласовать гибкость команды с потребностями большой организации.

**中文对应**

Disciplined Agile 将敏捷开发扩展到完整生命周期和组织层面，结合 Agile、Lean 与 UP 的思想，根据项目背景选择合适过程。

本题讨论的 DAD 生命周期有三个阶段：初始、构建、交付。除开发外，还考虑架构、复用、人员与项目组合管理、运行、支持和过程持续改进。目标是协调团队灵活性与大型组织的需要。

**原图参考 / Иллюстрации**

![Disciplined Agile 生命周期 / Жизненный цикл Disciplined Agile](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/dadLifecycleBasic.jpg)

---

### 第2题（原题 32）：Управления рисками. Деятельности, связанные с контролем и управлением.

**俄语答题 / Русский ответ**

Управление рисками включает планирование реакции, назначение ответственных, выполнение мер и постоянный мониторинг.

Стратегии: избегание — изменить план; снижение — уменьшить вероятность или ущерб; перенос — передать часть последствий другой стороне; принятие — осознанно оставить риск и подготовить резерв или план действий.

Риски регулярно переоценивают, контролируют индикаторы и обновляют TOP-10. Неопределённость снижают прототипами, моделированием, анализом и обучением; реестр можно вести в системе управления задачами.

**中文对应**

风险管理包括规划应对措施、指定负责人、实施措施和持续监控。

策略：规避，即改变计划；降低，即减少概率或损失；转移，即由其他方承担部分后果；接受，即有意识地保留风险，并准备储备或应急方案。

定期重新评估风险，监控指标并更新 TOP-10。通过原型、建模、分析和培训降低不确定性；可在任务管理系统中维护风险登记表。

---

### 第3题（原题 72）：Мониторинг производительности: Windows и Linux.

**俄语答题 / Русский ответ**

Windows: Task Manager показывает процессы и ресурсы; Resource Monitor — подробности CPU, памяти, дисков и сети; Performance Monitor — счётчики и их динамику; Reliability Monitor — историю сбоев. Sysinternals предоставляет дополнительные средства диагностики.

Linux: `top/pidstat` — процессы; `vmstat/free` — память и CPU; `mpstat` — отдельные CPU; `iostat` — устройства; `sar` — статистика системы; `perf` — профилирование; `strace` — системные вызовы. Средство выбирают по предполагаемому узкому месту и учитывают его накладные расходы.

**中文对应**

Windows：任务管理器显示进程和资源；资源监视器提供 CPU、内存、磁盘和网络细节；性能监视器记录计数器及其变化；可靠性监视器显示故障历史。Sysinternals 提供进一步诊断工具。

Linux：`top/pidstat` 看进程；`vmstat/free` 看内存和 CPU；`mpstat` 看各 CPU；`iostat` 看设备；`sar` 看系统统计；`perf` 做性能剖析；`strace` 跟踪系统调用。根据疑似瓶颈选择工具，并考虑测量开销。

**原图参考 / Иллюстрации**

![Linux 性能观测工具 / Инструменты наблюдаемости Linux](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/linux_perfomance_obervability_tools.png)

---

## Билет 16 · 变体 16

### 第1题（原题 10）：UML Диаграммы: Структурные и поведенческие. Примеры диаграмм.

**俄语答题 / Русский ответ**

UML — графический язык моделирования для описания, проектирования и документирования систем.

- Структурные диаграммы показывают статическое устройство: например, классов, объектов, компонентов, пакетов и размещения.
- Поведенческие диаграммы показывают поведение: например, вариантов использования, деятельности, состояний и последовательности. Диаграммы взаимодействия входят в поведенческие.

Пример: классы «Покупатель», «Заказ», «Товар» описывают структуру магазина; прецедент «Оформить заказ» — действие покупателя.

**中文对应**

UML 是用于描述、设计和记录系统的图形化建模语言。

- 结构图描述静态结构，例如类图、对象图、组件图、包图和部署图。
- 行为图描述行为，例如用例图、活动图、状态图和顺序图。交互图属于行为图。

例：买家、订单、商品三个类描述商店结构；“提交订单”用例描述买家的行为。

**原图参考 / Иллюстрации**

![UML 图分类 / Типы UML-диаграмм](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/uml_diagrams.png)

---

### 第2题：Формулирование требований. Функциональные требования. + Описание прецедента.

#### A（原题 25）：Формулирование требований. Функциональные требования.

**俄语答题 / Русский ответ**

Функциональные требования определяют, что система должна делать: функции, возможности, правила обработки данных и функции безопасности.

Форма: `<ID> <система> должна <действие при заданных условиях>`. Требование должно быть однозначным и проверяемым; условия и ожидаемый результат указываются явно.

Пример: «REQ-02: при вводе правильных учётных данных система должна открыть личный кабинет пользователя». Это требование к поведению, а не описание алгоритма реализации.

**中文对应**

功能需求规定系统应执行什么，包括功能、能力、数据处理规则和安全功能。

格式：`<编号> <系统> 应 <在指定条件下执行某动作>`。需求必须无歧义、可验证，并明确条件及预期结果。

例：“REQ-02：输入正确的登录凭据时，系统应打开用户个人页面。”这描述行为要求，而不是实现算法。

#### B（原题 29）：Описание прецедента.

**俄语答题 / Русский ответ**

Описание прецедента содержит ID, название, цель, основных и второстепенных акторов, предусловия, основной и альтернативные потоки, постусловия.

Пример «Просмотр статьи»: актор — читатель; предусловие — статья опубликована. Основной поток: читатель выбирает статью → система загружает и показывает текст. Альтернатива: статья недоступна → система сообщает об ошибке. Постусловие успешного выполнения — текст статьи показан читателю.

**中文对应**

用例说明包括编号、名称、目标、主要及次要参与者、前置条件、主流程、替代流程和后置条件。

例：“阅读文章”。参与者是读者；前置条件是文章已发布。主流程：读者选择文章→系统加载并显示正文。替代流程：文章不可访问→系统显示错误信息。成功后的后置条件是读者已看到文章正文。

**原图参考 / Иллюстрации**

![用例描述 / Описание прецедента](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/descr_case.jpg)

---

### 第3题（原题 50）：Системы сборки: GNU autotools. Конфигурация и сборка проекта.

**俄语答题 / Русский ответ**

При сборке готового исходного дистрибутива Autotools пользователь выполняет:

1. `./configure --prefix=/path` — проверить компилятор, библиотеки и функции; создать Makefile из `Makefile.in` и при необходимости `config.h` из `config.h.in`.
2. `make` — скомпилировать и связать программу.
3. `make install` — установить её в выбранный каталог.

Доступные параметры показывает `./configure --help`. Проверки зависят от `configure.ac`, описание целей — от `Makefile.am`. Повторная конфигурация адаптирует сборку к другой среде.

**中文对应**

使用 Autotools 源码发行包时，用户执行：

1. `./configure --prefix=/path` 检查编译器、库和函数，根据 `Makefile.in` 生成 Makefile；需要时根据 `config.h.in` 生成 `config.h`。
2. `make` 编译和链接程序。
3. `make install` 安装到指定目录。

`./configure --help` 显示配置选项。检查内容来自 `configure.ac`，构建目标描述来自 `Makefile.am`。重新配置可使构建适应不同环境。

**原图参考 / Иллюстрации**

![GNU Autotools 配置 / Конфигурация GNU Autotools](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/confs.png)

---

## Билет 17 · 变体 17

### 第1题（原题 11）：UML: Use-case модель. Элементы диаграммы, пример диаграммы.

**俄语答题 / Русский ответ**

Use-case диаграмма описывает функции системы с точки зрения внешних участников. Актор — внешняя роль; прецедент — цель взаимодействия. Актор изображается человечком, прецедент — овалом, граница системы — прямоугольником.

Связи: ассоциация — сплошная линия; `include` — обязательное включение, стрелка к включаемому прецеденту; `extend` — условное расширение, стрелка к базовому; обобщение — линия с пустым треугольником к родителю.

Пример: «Читатель» — «Вернуть книгу»; «Начислить штраф» `--extend-->` «Вернуть книгу» при просрочке. «Взять книгу» `--include-->` «Проверить читательский билет».

**中文对应**

用例图从外部参与者的角度描述系统功能。参与者是外部角色，用例是交互目标。参与者画小人，用例画椭圆，系统边界画矩形。

关系：关联用实线；`include` 表示必须包含，箭头指向被包含用例；`extend` 表示条件扩展，箭头指向基础用例；泛化用空心三角箭头指向父项。

例：“读者”—“还书”；逾期时，“计算罚款”`--extend-->`“还书”。“借书”`--include-->`“检查借书证”。

**原图参考 / Иллюстрации**

![Use-case 关系示例 / Пример Use-case](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/uml_morphysms.png)

---

### 第2题：Subversion: Основной цикл разработчика. Команды. + Subversion: Конфликты. Слияние изменений.

#### A（原题 36）：Subversion: Основной цикл разработчика. Команды.

**俄语答题 / Русский ответ**

Первую рабочую копию получают командой `svn checkout URL`. Затем цикл разработчика:

1. `svn update` — получить изменения.
2. Изменить файлы; `svn add/delete/move/copy` — зарегистрировать операции.
3. `svn status` и `svn diff` — проверить изменения; `svn revert` — отменить локальные правки.
4. Снова выполнить `svn update`, разрешить конфликты и протестировать.
5. `svn commit -m "message"` — отправить законченное изменение в репозиторий.

**中文对应**

首次通过 `svn checkout URL` 获取工作副本。之后的开发循环：

1. `svn update` 获取更新。
2. 修改文件，用 `svn add/delete/move/copy` 登记操作。
3. 用 `svn status` 和 `svn diff` 检查变化；`svn revert` 撤销本地修改。
4. 再次 `svn update`，解决冲突并测试。
5. 用 `svn commit -m "message"` 将完整修改提交到仓库。

**原图参考 / Иллюстрации**

![SVN 开发者工作循环 / Основной цикл разработчика SVN](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/svn_cycle.png)

#### B（原题 37）：Subversion: Конфликты. Слияние изменений.

**俄语答题 / Русский ответ**

Текстовый конфликт возникает, когда изменения одного участка нельзя автоматически объединить. Структурный конфликт связан с несовместимыми удалениями, перемещениями или изменениями файлов и каталогов.

При текстовом конфликте можно отложить решение (postpone), сравнить базовую, локальную и входящую версии, отредактировать итоговый файл, выполнить `svn resolve --accept working file`, протестировать и сделать commit. Файлы `.mine`, `.rOLD`, `.rNEW` помогают сравнению. Изменения между ветвями переносят командой `svn merge`; результат тоже проверяют и фиксируют.

**中文对应**

文本冲突指同一区域的修改无法自动合并。结构冲突指文件或目录的删除、移动、修改等操作互不兼容。

文本冲突时可先推迟处理（postpone），比较基线、本地和传入版本，编辑最终文件，执行 `svn resolve --accept working file`，测试后提交。`.mine`、`.rOLD`、`.rNEW` 文件辅助比较。用 `svn merge` 合并分支修改，合并结果也要检查并提交。

**原图参考 / Иллюстрации**

![SVN 冲突 / Конфликты SVN](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/conflict.png)

---

### 第3题：Аспекты быстродействия системы. Влияние средств измерения на результаты. + Пирамида памяти и ее влияние на производительность.

#### A（原题 65）：Аспекты быстродействия системы. Влияние средств измерения на результаты.

**俄语答题 / Русский ответ**

Производительность зависит от архитектуры системы, аппаратуры, алгоритмов и реализации, а также решений разработчиков. Важны распределение работы, память, I/O, конкуренция потоков и блокировки.

Измерение тоже потребляет ресурсы: меняет загрузку CPU, память и время выполнения. Средства бывают неинтрузивными, слабоинтрузивными и интрузивными. Нужно оценить накладные расходы, использовать одинаковую нагрузку и повторять измерения. Эксперимент: метрика → исходное измерение → одно изменение → повторная проверка результата.

**中文对应**

性能受系统架构、硬件、算法与实现以及开发决策影响。重点包括任务分配、内存、I/O、线程竞争和锁。

测量本身也消耗资源，会改变 CPU 负载、内存和执行时间。工具可分为非侵入、弱侵入和侵入式。应评估测量开销，使用相同负载并重复测量。实验流程：确定指标→测量基线→改变一个因素→再次验证结果。

#### B（原题 68）：Пирамида памяти и ее влияние на производительность.

**俄语答题 / Русский ответ**

Иерархия памяти: регистры → кэши L1/L2/L3 → RAM → SSD/HDD. Ниже по иерархии обычно больше объём и меньше стоимость хранения единицы данных, но выше задержка доступа.

Производительность повышает локальность: временная — повторное использование недавно прочитанных данных; пространственная — обращение к соседним адресам. Последовательный обход массива, компактные структуры и повторное использование данных уменьшают промахи кэша и медленные обращения. Подкачка на диск значительно увеличивает задержки.

**中文对应**

内存层次：寄存器→L1/L2/L3 缓存→RAM→SSD/HDD。层次越低，通常容量越大、单位存储成本越低，但访问延迟越高。

局部性有助于提升性能：时间局部性是复用最近访问的数据；空间局部性是访问相邻地址。顺序遍历数组、紧凑数据结构和数据复用可以减少缓存未命中及慢速访问。换页到磁盘会显著增大延迟。

**原图参考 / Иллюстрации**

![内存层次结构 / Пирамида памяти](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/memory_pyromid.png)

---

## Билет 18 · 变体 18

### 第1题（原题 12）：UML: Диаграмма классов. Элементы диаграммы, пример диаграммы.

**俄语答题 / Русский ответ**

Диаграмма классов показывает статическую структуру: классы, атрибуты, операции и отношения. Класс — прямоугольник с тремя секциями: имя / атрибуты / операции; `+` означает public, `-` — private.

Ассоциация — сплошная линия с кратностью (`1`, `0..*`); наследование — пустой треугольник к родителю; реализация — такой же треугольник с пунктиром; зависимость — пунктирная стрелка. Агрегация — пустой ромб у целого, части самостоятельны; композиция — закрашенный ромб, жизненный цикл частей связан с целым.

Пример: `Customer 1 — 0..* Order`; `Order 1 ◆— 1..* OrderLine`. У `Order`: `-id: int`, `+total(): double`.

**中文对应**

类图描述静态结构，包括类、属性、操作和关系。类画成三栏矩形：类名／属性／操作；`+` 表示 public，`-` 表示 private。

关联用实线，可标多重性（`1`、`0..*`）；继承用空心三角箭头指向父类；实现用虚线加空心三角；依赖用虚线箭头。聚合在整体端画空心菱形，部分可独立存在；组合画实心菱形，部分生命周期与整体相关。

例：`Customer 1 — 0..* Order`；`Order 1 ◆— 1..* OrderLine`。`Order` 包含属性 `-id: int` 和操作 `+total(): double`。

**原图参考 / Иллюстрации**

![类图示例 / Пример диаграммы классов](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/class_diagram_example.png)

---

### 第2题（原题 38）：GIT: Архитектура и команды.

**俄语答题 / Русский ответ**

Git — распределённая система контроля версий: у разработчика есть локальный репозиторий с историей. Основные области: рабочая директория → индекс (staging area) → локальный репозиторий → удалённый репозиторий.

Команды: `clone` — получить репозиторий; `init` — создать; `status/diff` — проверить изменения; `add` — подготовить; `commit` — сохранить локально; `push` — отправить. `fetch` получает удалённые изменения без их интеграции, `pull` получает и интегрирует. `branch` создаёт ветвь, `switch/checkout` переключает, `merge` объединяет. Возможны централизованный процесс, менеджер интеграции, диктатор и лейтенанты.

**中文对应**

Git 是分布式版本控制系统，开发者拥有保存历史的本地仓库。主要区域：工作目录→暂存区→本地仓库→远程仓库。

命令：`clone` 获取仓库；`init` 创建仓库；`status/diff` 检查修改；`add` 暂存；`commit` 保存到本地；`push` 推送。`fetch` 获取远程修改但不整合；`pull` 获取并整合。`branch` 创建分支，`switch/checkout` 切换，`merge` 合并。可采用集中式协作、集成经理、独裁者与副官等协作模式。

**原图参考 / Иллюстрации**

![Git 架构 / Архитектура Git](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/git_arch.png)

---

### 第3题：Автоматизация тестов и ручное тестирование. + Источники данных для тестирования. Роли и деятельности в тестировании.

#### A（原题 55）：Автоматизация тестов и ручное тестирование.

**俄语答题 / Русский ответ**

Ручное тестирование выполняет человек; оно удобно для исследовательских проверок, оценки удобства и редко повторяемых сценариев. Автоматизированное тестирование выполняют инструменты; оно удобно для повторяемых проверок, регрессии и нагрузки, но требует затрат на создание и сопровождение тестов.

Регрессионное тестирование проверяет, не нарушили ли изменения ранее работающие функции. Для надёжной автоматизации нужны управляемые данные и окружение, воспроизводимые действия и проверяемый ожидаемый результат. Совместимость проверяют в нескольких целевых средах.

**中文对应**

手工测试由人执行，适合探索性检查、易用性评价和很少重复的场景。自动化测试由工具执行，适合重复检查、回归和负载测试，但需要编写和维护成本。

回归测试检查修改是否破坏了原有功能。可靠的自动化需要可控的数据和环境、可重复的操作及可检查的预期结果。兼容性需要在多个目标环境中验证。

#### B（原题 56）：Источники данных для тестирования. Роли и деятельности в тестировании.

**俄语答题 / Русский ответ**

Источники тестов: требования, спецификации, проектные решения, исходный код, UML-модели и опыт. Чёрный ящик использует внешнее поведение и спецификацию; белый ящик — внутреннюю структуру кода.

В белом ящике граф потока управления помогает выбирать пути. Цикломатическая сложность характеризует число линейно независимых путей и используется для базисного тестирования, а не для подсчёта всех возможных сценариев.

Деятельности: проектирование тестов, автоматизация, выполнение и анализ результатов. Участвуют тестировщики, разработчики и специалисты предметной области.

**中文对应**

测试来源包括需求、规格说明、设计方案、源代码、UML 模型和经验。黑盒测试依据外部行为和规格；白盒测试依据代码内部结构。

白盒测试可用控制流图选择路径。圈复杂度表示线性独立路径的数量，用于基本路径测试，而不是计算所有可能的执行场景。

活动包括测试设计、自动化、执行和结果分析。参与者包括测试人员、开发者及领域专家。

---

## Билет 19 · 变体 19

### 第1题（原题 13）：UML: Диаграмма состояния. Элементы диаграммы, пример диаграммы.

**俄语答题 / Русский ответ**

Диаграмма состояний описывает состояния объекта и переходы между ними. Состояние — скруглённый прямоугольник; начало — чёрный круг; конец — круг с точкой внутри; переход — стрелка.

Метка перехода: `событие [условие] / действие`. В состоянии возможны действия `entry`, `do`, `exit`.

Пример: `● → New → Paid → Shipped → ◎`. Переход `New → Paid`: `pay [paymentOK] / savePayment`. Здесь `pay` — событие, `paymentOK` — условие, `savePayment` — действие.

**中文对应**

状态图描述对象的状态及状态之间的转换。状态画圆角矩形；开始画实心圆；结束画圆圈内套实心圆；转换画箭头。

转换标注：`事件 [条件] / 动作`。状态内部可写 `entry`、`do`、`exit` 动作。

例：`● → New → Paid → Shipped → ◎`。`New → Paid` 的标注为 `pay [paymentOK] / savePayment`。其中 `pay` 是事件，`paymentOK` 是条件，`savePayment` 是动作。

---

### 第2题：Maven: Структура проекта. GAV. + Maven: Зависимости. Жизненный цикл сборки. Плагины.

#### A（原题 46）：Maven: Структура проекта. GAV.

**俄语答题 / Русский ответ**

Стандартная структура Maven: `pom.xml` — описание проекта; `src/main/java` — основной код; `src/main/resources` — ресурсы; `src/test/java` — тесты; `src/test/resources` — тестовые ресурсы; `target` — результаты сборки.

GAV — координаты артефакта: `groupId:artifactId:version`. GroupId обозначает организацию или группу, artifactId — проект/артефакт, version — версию. Пример: `org.example:shop:1.0`. Координаты используются и для самого проекта, и для его зависимостей.

**中文对应**

Maven 标准结构：`pom.xml` 是项目描述；`src/main/java` 是主代码；`src/main/resources` 是资源；`src/test/java` 是测试；`src/test/resources` 是测试资源；`target` 是构建结果。

GAV 是制品坐标：`groupId:artifactId:version`。GroupId 表示组织或分组，artifactId 表示项目或制品，version 表示版本。例如 `org.example:shop:1.0`。项目本身及其依赖都使用坐标标识。

**原图参考 / Иллюстрации**

![Maven 项目结构 / Структура Maven-проекта](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/struct.png)

#### B（原题 47）：Maven: Зависимости. Жизненный цикл сборки. Плагины.

**俄语答题 / Русский ответ**

Зависимости объявляются в POM. Транзитивность: если A зависит от B, а B от C, A может получить C косвенно; это зависит от scope и исключений. Scope: `compile` — по умолчанию; `provided` — доступна при компиляции, предоставляется средой выполнения; `runtime` — выполнение; `test` — тесты; `system` — явно заданный локальный файл.

Жизненные циклы: clean, default, site. Основные фазы default: validate → compile → test → package → verify → install → deploy. Вызов фазы выполняет предыдущие фазы этого цикла. Плагин содержит цели (goals), которые выполняют работу и могут быть привязаны к фазам.

**中文对应**

依赖在 POM 中声明。传递性：若 A 依赖 B、B 依赖 C，A 可能间接获得 C，具体受 scope 和排除规则影响。Scope：`compile` 为默认；`provided` 在编译时可用，运行时由环境提供；`runtime` 用于运行；`test` 用于测试；`system` 使用明确指定的本地文件。

生命周期有 clean、default、site。Default 主要阶段：validate→compile→test→package→verify→install→deploy。调用某阶段会执行同一周期中此前的阶段。插件提供执行具体工作的目标（goals），目标可绑定到阶段。

**原图参考 / Иллюстрации**

![Maven 生命周期 / Жизненный цикл Maven](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/lifecycle.png)

![Maven 插件 / Плагины Maven](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/plugin.png)

---

### 第3题：Модульное тестирование. JUnit 4 с примерами. + Функциональное и End-to-End тестирование. Selenium.

#### A（原题 59）：Модульное тестирование. JUnit 4 с примерами.

**俄语答题 / Русский ответ**

Модульное тестирование проверяет отдельный метод, класс или компонент в изоляции. Драйвер вызывает модуль, заглушка (stub) заменяет его зависимость и возвращает подготовленные значения.

В JUnit 4 `@Test` отмечает тест, `assertEquals` проверяет результат. `@Before/@After` выполняются перед/после каждого теста, `@BeforeClass/@AfterClass` — один раз до/после тестов класса; последние методы должны быть `public static void`. Тесты не должны зависеть от порядка выполнения. Пример:

```java
import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class SumTest {
    static int add(int a, int b) { return a + b; }
    @Test public void addsTwoNumbers() {
        assertEquals(5, add(2, 3));
    }
}
```

**中文对应**

单元测试隔离检查一个方法、类或组件。驱动（driver）调用被测模块，桩（stub）替代其依赖并返回预设值。

JUnit 4 用 `@Test` 标记测试，用 `assertEquals` 检查结果。`@Before/@After` 在每个测试前后执行；`@BeforeClass/@AfterClass` 在整类测试前后各执行一次，后两者的方法必须是 `public static void`。测试不应依赖执行顺序。

上方完整例子定义加法函数，并断言 `add(2, 3)` 的结果为 `5`。

**原图参考 / Иллюстрации**

![Driver 与 Stub / Драйвер и заглушка](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/driver-stub.png)

![JUnit 4 框架示意 / Фреймворк JUnit 4](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/junit_framework.png)

#### B（原题 61）：Функциональное и End-to-End тестирование. Selenium.

**俄语答题 / Русский ответ**

Функциональное тестирование проверяет соответствие функций требованиям: при заданном вводе получается ожидаемый результат. Оно возможно на уровне API и интерфейса пользователя, вручную или автоматически.

End-to-End проверяет полный бизнес-процесс через все участвующие компоненты. Пример: вход → выбор товара → корзина → оплата → подтверждение заказа.

Selenium WebDriver управляет браузером: открывает страницы, находит элементы, вводит данные и нажимает кнопки. В тесте затем проверяется ожидаемый результат, например появление номера заказа.

**中文对应**

功能测试检查功能是否符合需求，即给定输入是否产生预期结果。可在 API 或用户界面层开展，既可手工也可自动化。

端到端测试检查贯穿所有参与组件的完整业务流程。例如登录→选商品→购物车→付款→确认订单。

Selenium WebDriver 控制浏览器，打开页面、定位元素、输入数据并点击按钮。之后在测试中检查预期结果，例如是否出现订单编号。

---

## Билет 20 · 变体 20

### 第1题（原题 14）：UML: Диаграмма размещения. Элементы диаграммы, пример диаграммы.

**俄语答题 / Русский ответ**

Диаграмма размещения показывает физическую архитектуру и размещение программных артефактов на узлах. Узел изображается объёмным прямоугольником. Узлы бывают устройствами (`device`) и средами выполнения (`executionEnvironment`); артефакт — файл программы или данных. Линии между узлами обозначают каналы связи.

Пример: клиентский компьютер — HTTPS — сервер приложения — SQL/TCP — сервер БД. На сервере приложения находится среда JVM и артефакт `shop.jar`, на сервере БД — СУБД и данные магазина.

**中文对应**

部署图描述物理架构及软件制品在节点上的部署。节点画成立体矩形，可分为设备（`device`）和执行环境（`executionEnvironment`）；制品是程序或数据文件。节点间连线表示通信通道。

例：客户端计算机—HTTPS—应用服务器—SQL/TCP—数据库服务器。应用服务器中有 JVM 执行环境和 `shop.jar` 制品；数据库服务器中有数据库管理系统和商店数据。

**原图参考 / Иллюстрации**

![部署图示例 / Пример диаграммы размещения](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/deployment_diagram.png)

---

### 第2题（原题 45）：Системы сборки: Maven. POM. Репозитории и зависимости.

**俄语答题 / Русский ответ**

Maven — декларативная система сборки, использующая соглашения о структуре проекта. `pom.xml` (Project Object Model) описывает координаты, упаковку, зависимости, плагины и профили.

Зависимости берутся из локального репозитория, а недостающие скачиваются из удалённых. `mvn package` создаёт пакет в `target`; `mvn install` также помещает его в локальный репозиторий; `mvn deploy` публикует в удалённый. Выполняемая фаза задаётся при запуске Maven.

**中文对应**

Maven 是采用约定项目结构的声明式构建系统。`pom.xml`（项目对象模型）描述坐标、打包类型、依赖、插件和配置方案。

依赖先从本地仓库获取，缺失时从远程下载。`mvn package` 在 `target` 中生成包；`mvn install` 还会将其安装到本地仓库；`mvn deploy` 发布到远程仓库。执行的阶段在运行 Maven 时指定。

**原图参考 / Иллюстрации**

![Maven 依赖/仓库示意 / Maven: зависимости и репозитории](https://raw.githubusercontent.com/alex-grandson/edu/main/MISPI/img/cycle.png)

---

### 第3题：Статическое и динамическое тестирование. + Техники статического тестирования. Статический анализ кода.

#### A（原题 54）：Статическое и динамическое тестирование.

**俄语答题 / Русский ответ**

Статическое тестирование проверяет артефакты без выполнения программы: требования, модели, документацию и код. Методы — рецензирование и автоматический статический анализ.

Динамическое тестирование запускает код с заданными данными и сравнивает фактический результат с ожидаемым. Пример: выполнить функцию и проверить возвращённое значение.

TDD: написать падающий тест → реализовать минимальный код для его прохождения → выполнить рефакторинг и повторить тесты (Red–Green–Refactor).

**中文对应**

静态测试不执行程序，而是检查需求、模型、文档和代码。方法包括评审和自动静态分析。

动态测试使用指定数据运行代码，将实际结果与预期结果比较。例如执行函数并检查返回值。

TDD：先编写失败的测试→实现使测试通过的最少代码→重构并重新测试，即红—绿—重构循环。

#### B（原题 62）：Техники статического тестирования. Статический анализ кода.

**俄语答题 / Русский ответ**

Статическое тестирование проверяет артефакты без выполнения программы. Основные техники: неформальное ревью, техническое и управленческое ревью, walkthrough (автор проводит участников по материалу), формальная инспекция с ролями, чек-листами и регистрацией дефектов.

Статический анализ автоматически исследует код или байткод: ищет неинициализированные значения, недостижимый код, нарушения типов и неправильное использование ресурсов. Примеры: Lint, FindBugs. Преимущество — раннее обнаружение дефектов и снижение стоимости исправлений; результаты требуют проверки.

**中文对应**

静态测试不运行程序，而是检查制品。主要技术：非正式评审、技术评审、管理评审、走查（作者带领参与者检查材料），以及包含角色、检查表和缺陷记录的正式审查。

静态分析自动检查源码或字节码，查找未初始化值、不可达代码、类型问题及资源使用错误。工具例：Lint、FindBugs。优点是尽早发现缺陷、降低修复成本；分析结果仍需核实。
