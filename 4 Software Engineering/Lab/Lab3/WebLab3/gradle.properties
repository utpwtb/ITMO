# ============================================================
# Project identification
# ============================================================
project.group=com.itmo
project.name=WebLab3
project.version=1.0-SNAPSHOT

# ============================================================
# Java configuration
# ============================================================
java.sourceCompatibility=17
java.targetCompatibility=17

# ============================================================
# SCP configuration (for scp target)
# ============================================================
scp.host=helios.cs.ifmo.ru
scp.port=2222
scp.user=s407960
scp.password=jhwp{8647
scp.remote.deploy.path=/home/s407960/wildfly-21.0.0.Final/standalone/deployments
scp.trust=true

# ============================================================
# Git configuration (for history / report / diff targets)
# These tasks use the local Git repository automatically
# ============================================================

# ============================================================
# Music configuration (for music target)
# ============================================================
music.file=lib/273187__nlm__alarm-pips.wav

# ============================================================
# Alternative build environment (for env target)
# ============================================================
alt.java.home=C:/Program Files/Java/jdk-21
alt.jvm.args=-Xmx512m -Dfile.encoding=UTF-8

# ============================================================
# Alternative version replacements (for alt target)
# Format: className.old=className.new
# ============================================================
alt.replace.class.PointBean=PointBeanAlt
alt.replace.class.ResultsBean=ResultsBeanAlt
alt.replace.class.Point=PointAlt
alt.replace.class.PointDao=PointDaoAlt
alt.replace.variable.submitSource=submitType

# ============================================================
# Diff exclusion classes (for diff target)
# ============================================================
diff.excluded.classes=PointBean

# ============================================================
# Localization (for native2ascii target)
# ============================================================
native2ascii.src.encoding=UTF-8
native2ascii.dest.encoding=ISO-8859-1

# ============================================================
# WAR manifest entries
# ============================================================
manifest.main.class=com.itmo.bean.PointBean
manifest.implementation.vendor=ITMO University
